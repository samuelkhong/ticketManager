<nav class="navbar navbar-expand navbar-dark bg-dark">
    <a class="navbar-brand" href="/">Ticket Management</a>
    <% if(user) { %>
      <span class="navbar-text text-light ml-3">Logged in as: <%= user.userName %></span>
    <% } %>
    <div class="navbar-collapse" id="navbarNav">
      <ul class="navbar-nav ml-auto">
        <li class="nav-item active">
          <a class="nav-link" href="/">Home</a>
        </li>
        <% if (!user) { %>
          <li class="nav-item">
            <a class="nav-link" href="/authenticate/login">Login</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/authenticate/register">Register</a>
          </li>
        <% } else { %>
          
          <li class="nav-item">
            <a class="nav-link" href="/authenticate/logout">Logout</a>
          </li>
        <% } %>
      </ul>
    </div>
  </nav>